# Dependencias del contenedor de despliegue (Render.com, runtime Docker).
# Solo lo que usa la API en ejecucion: ni matplotlib, ni shap, ni pyproj, ni
# pytest. Python 3.12. El campeon (modelo_campeon.joblib) se deserializa con
# ESTAS versiones exactas de scikit-learn / joblib / numpy.

fastapi==0.141.1
uvicorn[standard]==0.52.4
pydantic==2.13.5

scikit-learn==1.9.0
joblib==1.6.0
numpy==2.5.2
pandas==3.0.5
